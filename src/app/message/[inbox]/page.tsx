

export default async function Page({ params }: { params: { inbox: string } }) {
  return (
    <div className='w-full flex flex-col'>
      {/* <Suspense fallback={<MessagePageSkeleton />}>
        <MainClientPage data={data} />
      </Suspense> */}
    </div>
  )
}
